import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import Navigation from '@/components/Navigation';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Mohamet Gueye - AI Specialist & Digital Trainer',
  description: 'Portfolio of Mohamet Gueye - Specialist in AI, Prompt Engineering, and Digital Training. Experienced in software development, digital marketing, and generative AI technologies.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        {children}
      </body>
    </html>
  );
}
